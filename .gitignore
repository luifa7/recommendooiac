# Compiled files
*.tfstate
*.tfstate.backup

# Module-specific directories
.terraform/

# IDE files
.idea/

# Backup files
*.bak

# Crash log files
crash.log

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
*_override.tf
override.tf.json
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are configuration settings that are always local and different per user.
*.tfvars

# Ignore override files that are used to override default attribute values.
*.override.tf

# Ignore automatic files generated by Terraform
.tfstate.d/
.tfstate.lock.info
